#pragma once


namespace sltl
{
namespace api
{
  //TODO: move sltl::core namespace contents into a separate project and replace this enum with sltl::core::shader_stage
  enum class shader_stage
  {
    vertex,
    fragment
  };
}
}
